//determina que apenas um conjunto que deve ser validado - rexp
//novalidate n validar ao enviar
/*
const btn = document.querySelector('button');
const input = document.querySelector('input');

btn.addEventListener('click', () => {
    input.checkValidity
})
*/
//outpot - feedback p/ o usuário

//setInterval - executa uma função de tempos em tempos (param, time)